const cfg=document.getElementById("app-config");const SB_URL=cfg?.dataset.url||"";const SB_KEY=cfg?.dataset.key||"";
async function api(path,options={}){const r=await fetch(path,{credentials:"same-origin",headers:{"Content-Type":"application/json",...(options.headers||{})},...options});if(!r.ok){let x={};try{x=await r.json()}catch{}throw new Error(x.error||`HTTP ${r.status}`)}return r.status===204?null:r.json()}
async function loadWeeks(){const el=document.getElementById("weeks");if(!el)return;try{const weeks=await api("api/weeks");el.innerHTML=weeks.map(w=>`<a class="card" href="semana.jsp?n=${w.week_number}"><h2>Semana ${w.week_number}</h2><p>${esc(w.title||"Contenido académico")}</p></a>`).join("")}catch(e){el.innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
async function loadWeek(){const el=document.getElementById("week-detail");if(!el)return;const n=Number(el.dataset.week);try{const data=await api(`api/resources?week=${n}`);el.innerHTML=`<div class="section-head"><div><p class="eyebrow">SEMANA ${n}</p><h1>${esc(data.week.title)}</h1><p>${esc(data.week.description||"")}</p></div></div><div class="resource-list">${data.resources.length?data.resources.map(resourceHtml).join(""):"<div class='empty'>No hay recursos publicados.</div>"}</div>`}catch(e){el.innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
async function loadActivities(){const el=document.getElementById("activities");if(!el)return;try{const xs=await api("api/resources?type=activity");el.innerHTML=xs.length?xs.map(resourceHtml).join(""):"<div class='empty'>No hay actividades.</div>"}catch(e){el.innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
async function loadProfile(){const el=document.getElementById("profile");if(!el)return;try{const p=await api("api/profile");el.innerHTML=`<div class="profile"><div>${p.photo_url?`<img src="${escAttr(p.photo_url)}" alt="Foto del estudiante">`:""}</div><div><p class="eyebrow">PERFIL</p><h1>${esc(p.name||"Estudiante")}</h1><h2>${esc(p.career||"")}</h2><p>${esc(p.description||"")}</p></div></div>`}catch(e){el.innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
function resourceHtml(r){const action=r.public_url?`<a class="button secondary" target="_blank" rel="noopener" href="${escAttr(r.public_url)}">${r.file_name?"Ver / descargar":"Abrir enlace"}</a>`:"";return `<article class="resource"><div><strong>${esc(r.title)}</strong><p>${esc(r.description||"")}</p><small>Semana ${r.week_number} · ${esc(typeLabel(r.type))}${r.file_name?" · "+esc(r.file_name):""}</small></div>${action}</article>`}
function typeLabel(type){return ({document:"Documento",activity:"Actividad",link:"Enlace"}[type]||type||"")}
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]))}function escAttr(s){return esc(s)}
loadWeeks();loadWeek();loadActivities();loadProfile();
