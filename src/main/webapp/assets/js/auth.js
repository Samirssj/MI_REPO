document.addEventListener("DOMContentLoaded",async()=>{const cfg=document.getElementById("app-config");const msg=document.getElementById("login-message");if(!window.supabase){msg.textContent="Falta cargar Supabase JS. Revisa SETUP.md.";return}const client=window.supabase.createClient(cfg.dataset.url,cfg.dataset.key);const form=document.getElementById("login-form");form.addEventListener("submit",async e=>{e.preventDefault();msg.textContent="Ingresando...";const {data,error}=await client.auth.signInWithPassword({email:email.value.trim(),password:password.value});if(error){msg.textContent=error.message;return}const r=await fetch("api/auth/session",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({access_token:data.session.access_token})});if(!r.ok){msg.textContent="La cuenta no tiene permisos de administrador.";await client.auth.signOut();return}location.href="admin.jsp"});});